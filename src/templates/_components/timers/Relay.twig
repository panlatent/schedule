{% import "_includes/forms" as forms %}

{{ forms.textField({
    label: 'Waiting time (minute)'|t('schedule'),
    instructions: 'Wait a few minutes after the schedule is completed and then execute it.'|t('schedule'),
    required: true,
    id: 'wait',
    name: 'wait',
    value: timer.wait,
    size: 10,
    errors: timer.getErrors('wait'),
}) }}